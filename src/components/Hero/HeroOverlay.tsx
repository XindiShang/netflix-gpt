const HeroOverlay = () => {
  // If enable user to play with youtube player, change top-0 to -top-10, and also change content component
  return (
    <div className="absolute top-0 left-0 w-full h-full bg-gradient-to-b from-transparent to-black opacity-70"></div>
  );
};

export default HeroOverlay;
